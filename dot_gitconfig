[core]
    excludesfile = ~/.gitignore
    pager = less --quit-if-one-screen --no-init

[pull]
    rebase = true

[push]
    default = simple

[color]
    ui = auto

[include]
    path = ~/.gitconfig_local
